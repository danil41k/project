# 🐳 Flask Shop API - Контейнеризація проєкту

## 📋 Огляд проєкту

**Flask Shop API** — це повнофункціональна вебзастосунок для управління магазином електронних книжок з REST API.

### Функціональність:
- **Керування товарами**: додавання, оновлення, видалення, пошук за фільтрами (ціна, назва, фото)
- **Управління замовленнями**: створення, пошук за email, отримання деталей, оновлення статусу
- **Система відгуків**: додання та видалення відгуків, розділення на типи (загальні, для розробника)
- **REST API**: повнофункціональне API з документацією Swagger (Flasgger)
- **API Demo**: інтерактивна веб-сторінка для тестування всіх ендпоінтів
- **Адмін-панель**: повне управління товарами, замовленнями, клієнтами та відгуками
- **Аутентифікація**: захищені адмін-панель та API Demo з паролями

---

## 🏗️ Архітектура контейнерного рішення

### Docker образ

- **Базовий образ**: `python:3.11-alpine` (легкий ~50 MB)
- **Розмір фінального образу**: ~260-300 MB (з усіма залежностями)
- **Використання багатоетапної збірки**: ✅ Так (зменшує розмір на ~100 MB)
- **Непривілейований користувач**: ✅ Так (appuser)
- **Health check**: ✅ Включено (перевіряє `/api/v1/health`)

### Docker Compose (Production)

- **Кількість сервісів**: 3
  1. **web** — Flask застосунок (Python 3.11 Alpine)
  2. **backup** — автоматичний backup SQLite (shell script)
  3. **nginx** — reverse proxy, compression, SSL ready (nginx Alpine)

- **Використовувані volumes**:
  - `sqlite_data` — персистентність SQLite БД (`/app/data`)
  - `app_logs` — логи додатку (`/app/logs`)
  - `app_backups` — резервні копії БД (`/app/backups`)

### Docker Compose (Development)

- **Кількість сервісів**: 1
  1. **web** — Flask з live reload та debug mode

---

## 🎯 Прийняті рішення та обґрунтування

### Вибір базового образу

**Обрано**: `python:3.11-alpine` (для production) + `python:3.11-slim` (для builder)

**Обґрунтування**:
- Alpine — найлегший образ (~50 MB базовий розмір)
- Швидке завантаження та розгортання
- Достатньо мініму утиліт для production
- Безпека: регулярно оновлюється
- Сумісність з Python 3.11

**Альтернативи розглянуті**:
- `python:3.11-slim` — більший (~165 MB), але містить більше інструментів
- `python:3.11` — надто великий (~900 MB), не потрібен для production

### Організація збереження даних

**Архітектура**:
- Використання **Docker volumes** для персистентності даних
- Розділення даних на **системні** (код, залежності) та **змінні** (база даних, логи, файли користувача)
- Автоматичне створення та монтування томів через Docker Compose

**Список томів**:
- `sqlite_data`: для зберігання бази даних SQLite
- `app_logs`: для зберігання логів додатку
- `app_backups`: для резервних копій бази даних

### Вибір стратегії резервного копіювання

**Обрана стратегія**:
- Автоматичне щоденне резервне копіювання бази даних
- Зберігання резервних копій у окремому томі (`app_backups`)
- Використання скриптів для управління резервними копіями

**Обґрунтування**:
- Захист даних від втрати
- Легкість відновлення даних у разі потреби
- Мінімізація ручних операцій

---

## 🚀 Інструкція з розгортання

### Передумови

- Встановлений [Docker](https://docs.docker.com/get-docker/) та [Docker Compose](https://docs.docker.com/compose/install/)
- Основи роботи з командним рядком

### Кроки розгортання

1. **Клонувати репозиторій**:
   ```bash
   git clone https://github.com/yourusername/flask-shop-api.git
   cd flask-shop-api
   ```

2. **Запустити в продакшн-режимі**:
   ```bash
   docker-compose up -d --build
   ```

3. **Або запустити в режимі розробки**:
   ```bash
   docker-compose -f docker-compose.dev.yml up -d --build
   ```

4. **Перевірити статус контейнерів**:
   ```bash
   docker-compose ps
   ```

5. **Доступ до застосунку**:
   - Web: `http://localhost`
   - Swagger UI: `http://localhost/apidocs`
   - Admin Panel: `http://localhost/admin`

6. **Зупинити контейнери**:
   ```bash
   docker-compose down
   ```

---

## 📚 Документація та корисні посилання

- [Документація Flask](https://flask.palletsprojects.com/)
- [Документація Docker](https://docs.docker.com/)
- [Документація Docker Compose](https://docs.docker.com/compose/)
- [Документація Swagger (Flasgger)](https://flasgger.readthedocs.io/en/latest/)

---

## 🛠️ Технології та інструменти

- **Мови програмування**: Python 3.11
- **Фреймворки**: Flask
- **Бази даних**: SQLite
- **Контейнеризація**: Docker, Docker Compose
- **Документація API**: Swagger (Flasgger)
- **Системи контролю версій**: Git

---

## 👤 Автори

- **Остаповець Данило та Козачук Максим** -  - [Мій GitHub](https://github.com/danil41k/project)

---

## 📅 Дата завершення

- 2025.11.12

